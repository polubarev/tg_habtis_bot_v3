## 1. Implementation
- [ ] 1.1 Add default value capture to the habit field add flow, including type/min/max validation and persistence.
- [ ] 1.2 Add a default attribute to the habit field edit flow (keyboard + prompts), with validation and clear/reset behavior.
- [ ] 1.3 Extend habit JSON import and field details display to include default values.
- [ ] 1.4 Apply defaults when extracted habit values are None and track which fields were defaulted.
- [ ] 1.5 Update habit draft preview formatting to show default markers (RU/EN) only for defaulted fields.
- [ ] 1.6 Ensure sheet writes use actual default values without any preview marker suffix.

## 2. Tests
- [ ] 2.1 Add or adjust tests for default validation, default application, and preview formatting if test harness exists.
