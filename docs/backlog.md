# Backlog

- Сделать это кнопками: показать пользователю выбор типа поля через варианты
  "string — текст (например, настроение)", "int — целое число (например, 3)",
  "float — число с точкой (например, 2.5)", "bool — да/нет (например, выпил витамины)".
- Для удаления показывать текущий список.
- После добавления привычки показывать кнопки из меню привычек.
- Убирать сообщения вроде "⏳ Обрабатываю..." и "Сохраняю данные..." после использования.
